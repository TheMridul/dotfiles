# Shared agent resources

Use `~/.agents/` for user-level skills that are useful across projects.
Project-specific context belongs in the project root's `AGENTS.md`; reusable
project resources belong in that project's `.agents/` directory.

Keep provider-specific configuration in its own tool directory. Do not copy
project rules into `~/.agents/`.
